# Markaz Imam Ahmad bin Hanbal — Secure Node.js Conversion

Run:
```bash
npm install
npm start
```

Then open `http://localhost:3000`.

Default admin bootstrap is server-side only. The app seeds a protected admin account on first run and stores all content on the server, not in browser localStorage.
